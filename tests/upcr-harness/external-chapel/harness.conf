BEGIN_DEFAULT_CONFIG
MakeFlags:      
Flags:          
Files:          
DynamicThreads: $DEFAULT$
StaticThreads:  0
CompileResult:  pass
PassExpr:       0
FailExpr:       ERROR
ExitCode:       0
BuildCmd:       make,notrans
AppArgs:        
RunCmd:         %P -nl %N
TimeLimit:      $DEFAULT$
RequireFeature: gasnet,chapel
AppEnv:         network_udp; GASNET_QUIET=1
# Sigh - builds are far too noisy to apply precise filters
WarningFilter:  .*?(chpl__auto_destroy_Error|error|warning|WARNING).*?
END_DEFAULT_CONFIG

# ------------------------------------------
# Tests to compile, but not run

TestName:	chapel
TimeLimit:      0
CompileTimeLimit: 2 * $DEFAULT$
# TODO: figure out how to enable the following without hitting
#error Tried to compile GASNet client code with optimization enabled but also GASNET_DEBUG [...]
#MakeFlags:      debug; CHPL_GASNET_MORE_CFG_OPTIONS=--enable-debug

# ------------------------------------------
# General tests 

TestName:       example-hello6-taskpar-dist
PassExpr:       from locale 0 of

